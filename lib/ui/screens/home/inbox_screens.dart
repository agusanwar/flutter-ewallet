import 'package:flutter/material.dart';

class InboxScreens extends StatelessWidget {
  const InboxScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('inbox Screens'),
    );
  }
}
